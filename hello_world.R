# Print "Hello, World!"
print("Hello, World!")

# Create a data frame with rock band information
rock_bands <- data.frame(
    band = c("AC/DC", "Led Zeppelin", "Queen"),
    country = c("USA", "UK", "Germany"),
    tour_date = as.Date(c("2022-01-01", "2022-02-01", "2022-03-01"))
)

# Print the dataset
print(rock_bands)